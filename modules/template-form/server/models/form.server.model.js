'use strict';

/**
 * Module dependencies.
 */
var mongoose = require('mongoose'),
  Schema = mongoose.Schema;

/**
 * Revenue Schema
 */
var FormSchema = new Schema({
  // Revenue model fields
  // ...
});

mongoose.model('TemplateFormForm', FormSchema);
